#include <stdio.h>
/**
 * main -  prints a[2] = 98, followed by a new line.
 * @a: integer value
 * Return: 0
 */
int main(int a)
{
	int a[] = {4, 8, 98, 12, 67};
	int *p = &a{0];

	printf("a[2] = %di\n", *(p + 2);

	return (0);
}
