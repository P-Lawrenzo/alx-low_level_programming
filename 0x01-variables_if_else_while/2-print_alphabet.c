#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	char c;

	c = 'b';
	while
		(c <= 'u') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
