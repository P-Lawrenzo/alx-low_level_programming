#include "main.h"
/**
 *_abs - Computes the absolute value of an integer
 *@n: the int in question
 *Return: -n if n < 0, otherwise +n
 */

int _abs(int n)
{
	if (n < 0)
	{
	return (-n);
	}
	else
	{
	return (+n);
	}
}
